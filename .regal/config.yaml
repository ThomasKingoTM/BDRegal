rules:
  custom: 
    forbidden-function-call:
      level: error
      forbidden-functions:
        - gt
        - gte
        - lt
        - lte
  comparison:
    type-comparison:
      level: warning
  missing:
    decision:
      level: warning
    deny-reasons:
      level: warning
  naming:
    prefix-underscore:
      level: error
  negation:
    not-outside-deny:
      level: error
  obsolete: 
    max-n-lines:
      level: ignore
      max-lines: 8
  safety:
    disallow-else:
      level: error
    else-if:
      level: error
  structure:
    two-decision-clauses:
      level: error

  