package util

safe_gt(x, y) := x > y

if {
	is_number(x)
	is_number(y)
}

safe_lt(x, y) := x < y

if {
	is_number(x)
	is_number(y)
}

safe_ge(x, y) := x >= y

if {
	is_number(x)
	is_number(y)
}

safe_le(x, y) := x <= y

if {
	is_number(x)
	is_number(y)
}
